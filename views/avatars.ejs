<!DOCTYPE html>
<html>
<head>
    <% include ../views/partials/head.ejs %>
</head>
<body page-type="users">
<% include ../views/partials/header.ejs %>
<div class="container">
    <h1><%= title %></h1>
    <div class="container">
        <% if (avatars.length) { %>
            <% for(var i=0; i<avatars.length; i++) { %>
                <img src="<%= '/uploads/avatars/' + avatars[i] %>" alt=""/>
            <% } %>
        <% } %>
    </div>
</div>
</body>
</html>
